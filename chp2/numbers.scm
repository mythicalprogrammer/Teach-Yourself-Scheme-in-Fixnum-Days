; Simple data types (Numbers)
(begin
  (display "Scheme Numbers can be integers(42), rationals(22/7), reals(3.1416), or complex(2+3i).")
  (newline))
(begin
  (display "---------")
  (newline))
(begin
  (display "number? 42:")
  (newline))
(begin
  (display (number? 42))
  (newline))
(begin
  (display "---------")
  (newline))
(begin
  (display "number? #t:")
  (newline))
(begin
  (display (number? #t))
  (newline))
(begin
  (display "---------")
  (newline))
;(begin
;  (display "complex? 2+3i:")
;  (newline))
;(begin
;  (display (complex? 2+3i))
;  (newline))
;(begin
;  (display "number? 2+3i:")
;  (newline))
;(begin
;  (display (number? 2+3i))
;  (newline))
(begin
  (display "---------")
  (newline))
(begin
  (display "rational? 3.1416:")
  (newline))
(begin
  (display (rational? 3.1416))
  (newline))
(begin
  (display "real? 3.1416:")
  (newline))
(begin
  (display (real? 3.1416))
  (newline))
(begin
  (display "integer? 3.1416:")
  (newline))
(begin
  (display (integer? 3.1416))
  (newline))
(begin
  (display "number? 3.1416:")
  (newline))
(begin
  (display (number? 3.1416))
  (newline))
(begin
  (display "---------")
  (newline))